<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#000"
    tools:context="com.marin.saul.magicdice.MainActivity">

    <ImageView
        android:id="@+id/activity_main_number"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:gravity="center"
        android:textSize="200dp"/>

    <TextView
        android:id="@+id/activity_main_number_text"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        tools:text="100"
        android:gravity="center"
        android:textColor="#f00"
        android:background="@drawable/rounded_corner"
        android:textSize="160dp"/>

    <Button
        android:id="@+id/activity_main_roll_button"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:textSize="25dp"
        android:layout_marginTop="30dp"
        android:textColor="#fff"
        android:background="#444"
        android:text="@string/roll_the_dice"
        android:layout_below="@id/activity_main_number"/>

    <ToggleButton
        android:id="@+id/activity_main_normal_button"
        android:layout_width="150dp"
        android:layout_height="60dp"
        android:textOn="@string/normal_dice"
        android:textColor="#fff"
        android:textOff="@string/normal_dice"
        android:layout_below="@id/activity_main_roll_button"/>

    <ToggleButton
        android:id="@+id/activity_main_percentil_button"
        android:layout_width="150dp"
        android:layout_height="60dp"
        android:textOn="@string/percentile_dice"
        android:textOff="@string/percentile_dice"
        android:textColor="#fff"
        android:layout_below="@id/activity_main_roll_button"
        android:layout_toRightOf="@id/activity_main_normal_button"/>


</RelativeLayout>
